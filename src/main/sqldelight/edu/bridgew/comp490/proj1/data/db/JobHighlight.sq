CREATE TABLE IF NOT EXISTS JobHighlightDAO (
    title TEXT,
    jobId TEXT NOT NULL,
    item TEXT NOT NULL,
    PRIMARY KEY (title, jobId, item),
    FOREIGN KEY (jobId) REFERENCES JobDAO(jobId)
);
