CREATE TABLE QueryDAO (
    query TEXT NOT NULL,
    jobId TEXT NOT NULL,
    UNIQUE (query, jobId),
    FOREIGN KEY (jobId) REFERENCES JobDAO(jobId)
);

CREATE INDEX IF NOT EXISTS search_query_idx ON QueryDAO(query);